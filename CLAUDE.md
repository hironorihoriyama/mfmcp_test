# 共通の安全ルール
- 過去のパターンにない新規の取引先や、判断の確信度が低いものは自動判定せず、スキップして「未入力」のままにし、人間に確認を回す。

# テスト運用中の安全ルール（厳守）
- 処理対象のデータは「2026年度」のデータのみとする。
- 処理対象は「PayPayカード」の未処理明細のみに限定する。名前での曖昧な判定は行わず、必ず `CLAUDE.local.md` に記載されている固有の「補助科目ID」をデータ抽出の条件として使用すること。
- 処理対象の件数は、取得した未処理明細のうち「最初の10件のみ」とする。
- それ以外の年度や、他の口座・カードのデータ、および11件目以降のデータは一切処理しないこと。
- MFCへの仕訳の直接登録（更新）は行わず、必ずドラフト（提案）としてターミナル上に出力し、ユーザーの確認を待つこと。